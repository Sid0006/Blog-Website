<%- include('partials/header'); -%>

    <form class="home" action="/">
        <h1>HOME</h1>

        <p>
            <%= homeContent %>
        </p>
        <% posts.forEach(function(post){ %>
            <h1>
                <%= post.title %>
            </h1>
            <p>
                <%= post.content.substring(0, 100) + "..." %> <a href="/posts/<%= post.title %>">Read more..</a>
            </p>

            <% }) %>
                <a class="a-1" href="/compose"><img class="img-1" src="/images/_plus_add_icon.png" alt=""></a>

    </form>
    <%- include('partials/footer'); -%>